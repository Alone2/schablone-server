version: '3'
services:
  schablone-server:
    build: ./
    restart: always
    environment:
      PORT: 8080
    ports:
      - 8080:8080

  mariadb:
    build: ./db
    restart: always
    env_file:
      - .mariadb.env
